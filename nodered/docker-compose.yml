services:
  node-red:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TZ=America/Sao_Paulo
    ports:
      - 1880:1880
    networks:
      - node-red-net
    restart: no
    volumes:
      - ./home/data:/data

networks:
  node-red-net: